# 2026-05-27

## Fork + rebrand from md-editor-plus

새 프로젝트로 [md-editor-plus](https://github.com/aviranrevach/md-editor-plus) 를 fork. upstream 추적 (`git remote` → `upstream`), 새 `origin` 은 GitHub repo 생성 시 연결 예정.

- 식별자 리브랜딩: `md-editor-plus`/`mdEditorPlus` → `notion-markdown-editor`/`notionMdEditor`, viewType `notionMdEditor.editor`, 표시명 `Notion Markdown Editor`, publisher `jihan`, 클래스/파일 `NotionMarkdownEditorProvider`(`notionMarkdownEditorProvider.ts`), 로그태그 `[notion-md-editor]`.
- `package.json` name/version(0.0.1)/author/URL → 2JIHAN repo.
- 검증: `npm run compile` 통과. `npm test` 22/22 (단, `toggle.test.ts` 는 **upstream 기존** ts-jest 타입에러로 fail — 리브랜딩과 무관, `npm run compile` 은 통과).

## Callout: GFM → Notion-flavored `<callout>` 포팅

[docs/adr/0001](../adr/0001-notion-flavored-callout.md) 결정 반영. callout 을 `> [!NOTE]` 에서 Notion API 의 `<callout icon="…" color="…">` 형식으로 전환.

- `src/webview/extensions/callout.ts` — 노드 속성 `emoji`+`color`(18색), `content: block+`, `preprocessMarkdownCallouts` 가 `<callout>` → `<div data-callout>` (nested 재귀, 내부 markdown 은 markdown-it 로 렌더 후 `&#10;` 인코딩해 단일 라인 → CommonMark blank-line 종료 회피, 코드블록 빈 줄 보존), serialize 는 `state.renderContent` 로 자식 블록 왕복.
- `src/webview/calloutMenu.ts` — `type` 제거, 프리셋 칩(emoji+color) + 18색 스와치 그리드 + 커스텀 이모지.
- `src/webview/blockPicker.ts` — 슬래시 callout 을 `CALLOUT_PRESETS` 기반으로, insert content `block+` 대응(`paragraph`), `convertToCallout` 헬퍼(inline → paragraph 래핑).
- `src/webview/styles/*.css` — callout 스타일을 `data-type` → `data-color` 19색 × 4테마(light/dark/claude/sepia), 메뉴 스와치 스타일 추가.
- `tests/callout.test.ts` — preprocess 파스 방향 8케이스 + 프리셋/색상, 10/10 통과.
- 의존성: `markdown-it`(직접 import 명시) + `@types/markdown-it`, `punycode`(esbuild browser 번들용 userland shim).

### 빌드 함정 기록

`markdown-it` 를 webview 번들에 직접 import 하자 esbuild(`platform: browser`)가 `require('punycode')` 에서 멈춤(context API 에선 error 가 hang 으로 표출). tiptap-markdown 은 pre-bundled dist 라 그동안 안 드러났음. userland `punycode` 설치로 해결.

## GitHub origin push + 배포 준비

- `.gitignore` 에 `.omc/` 추가 (OMC 로컬 상태 — 머신 종속 절대경로/세션 캐시, 커밋 대상 아님). `.codeboarding/` 디렉터리는 안 쓰므로 삭제.
- [README.md](../../README.md) "Getting started" 에 `git clone` → `npm install` → `npm run compile` → F5, 개발 중 `watch`/`test`, 작업 후 commit/push 흐름 추가 (다른 머신에서 이어가는 절차).
- `origin`(2JIHAN repo) 으로 push.

## upstream 브랜드 냄새 제거

Marketplace 수동 업로드 시 두 가지 막힘 해결.

- **displayName 충돌.** `"Notion Markdown Editor"` 가 Marketplace 전역 선점됨 → `"Notion WYSIWYG Markdown Editor"` 로 변경 ([package.json](../../package.json) line 3, customEditor 내부 라벨 line 52 는 충돌 없어 유지).
- **`.vscodeignore` 누락으로 `.omc/` 가 vsix 에 실림.** vsce 는 `.gitignore` 가 아니라 `.vscodeignore` 를 봄. `.omc`, `**/.omc/**`(중첩 대응 — `cd media` 잔류 cwd 탓에 훅이 `media/.omc/` 를 만든 사례 있음), `.codeboarding` 추가.

upstream 자산/명칭 정리 (LICENSE 원저작권 + 포크 attribution 은 MIT 의무로 유지).

- 삭제: `media/MD-editor-plus.png`(upstream 히어로, vsix 에 실리던 것), `docs/superpowers/`(upstream 옛 plans+specs 11개).
- [CHANGELOG.md](../../CHANGELOG.md) — upstream 0.1.0~0.2.0 릴리스 히스토리(제품명 "MD Editor Plus", 옛 `mdEditorPlus.*`) → 0.0.1 초기 릴리스 단일 항목으로 리셋. 현재 설정 네임스페이스 `notionMdEditor.*` 반영.
- [README.md](../../README.md) — 히어로 이미지 참조 제거(기능 스크린샷 3개는 유지). 포크 크레딧 프로즈 유지.
- [PUBLISHING.md](../../PUBLISHING.md) — `AviranRevach`/`md-editor-plus` 게시 절차 → `jihan`/`notion-markdown-editor` 재작성, 수동 업로드 절 추가.
- `demo.md`, `demo-rtl.md` — 제목, 개인 이메일(`aviran@atera.com`), 옛 설정/repo 링크, `author: Aviran` 스크럽.
- 검증: `npx vsce ls` 로 최종 패키지 확인 — `.omc`/codeboarding/히어로/src/tests/맵 0건. 텍스트 브랜드 토큰 0건(포크 크레딧만 의도 유지).
- **아이콘 교체**: upstream M↓ 로고 → 사용자 제공 ".md" 박스 아이콘으로 교체. `media/icon.png`(실제 래스터 256px, package.json `icon`) 만 유지. 벡터 재구성(`media/icon.svg`) 은 사용자가 반려해 삭제 — `.png` 단일 에셋만 둠. `media/icon.svg` 참조처(demo.md, .vscodeignore) 정리.

## 남은 주의점

- **serialize(편집 결과 → markdown) 방향은 런타임 QA 필요.** 단위 테스트는 node 환경이라 에디터를 못 띄워 parse 방향만 검증함. VSCode 에서 F5(또는 Cmd+R 리로드)로 `.md` 열어 callout 편집 → 저장 → `<callout icon color>` 왕복 확인 필요. (확장 호스트 kill 금지, 리로드로.)
- Notion sync 는 아직 미착수(그린필드, 별도 설계 필요).
